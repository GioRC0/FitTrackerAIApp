// lib/config/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Color Primario de Acción (Verde Turquesa)
  static const Color primaryAction = Color(0xFF00ADB5); // Un tono de Verde Turquesa

  // Color de Alerta y Error (Rojo)
  static const Color alertError = Color(0xFFE94560); 

  // Colores Base para Modos
  static const Color lightBackground = Colors.white; // Fondo claro
  static const Color darkBackground = Color(0xFF222831); // Gris oscuro para fondo oscuro
  static const Color lightText = Colors.black87; // Letras oscuras (casi negro)
  static const Color darkText = Colors.white; // Letras blancas
  
  // Color Neutro (Gris claro/medio) para elementos secundarios
  static const Color neutralSecondary = Color(0xFFEEEEEE);
}